import React, { Component } from "react";
import logos from "../../logos";

export default class TeamSelect extends Component {
  render() {
    return (
      <div>
        <p>Team Select Page</p>
      </div>
    );
  }
}
